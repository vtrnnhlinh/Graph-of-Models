name: qwen_lora
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - cudatoolkit=11.8
  - pip:
      - torch==2.1.0
      - transformers>=4.40.0
      - datasets
      - peft
      - accelerate
      - bitsandbytes
      - trl

